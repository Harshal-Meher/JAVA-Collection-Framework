import java.util.HashMap;import java.util.Map;import java.util.stream.Collectors;public class NTHSalaryAbove {    public static void main(String[] args) {        Map<String, Integer> employees = new HashMap<>();        employees.put("Harshal", 50000);        employees.put("Rishi", 40000);        employees.put("Ram Charan", 90000);        employees.put("Akash", 30000);        int threshold = 45000;//Above Salary        Map<String,Integer> res = filterEmployeesAboveThreshold(employees , threshold);        System.out.println("Employees with salaries above = 45000  : "+res);    }    public static Map<String, Integer>filterEmployeesAboveThreshold(Map<String, Integer> employees, int threshold) {        return employees.entrySet().stream().filter(e -> e.getValue() > threshold).collect(Collectors.toMap(Map.Entry:: getKey ,Map.Entry::getValue));    }                                     //use filter}