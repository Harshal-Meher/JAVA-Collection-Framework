import java.util.Collections;import java.util.HashMap;import java.util.Map;import java.util.stream.Collectors;public class NTHSalary {    public static void main(String[] args) {        Map<String, Integer> map1 = new HashMap<>();        map1.put("Harshal", 30000);        map1.put("Roshan", 20000);        map1.put("AkKi", 40000);        map1.put("Akash", 50000);        map1.put("OM", 90000);        Map.Entry<String,Integer> result =                map1.entrySet()                        .stream()                        .sorted(Map.Entry.comparingByValue()) // Sort in ascending order//                        .sorted(Collections.reverseOrder(Map.Entry.comparingByValue())) // Sort in decending order                        .collect(Collectors.toList())                        .get(1); // Get the second lowest entry        System.out.println("Employee with the second-lowest salary: " + result.getKey() + " = " + result.getValue());    }}